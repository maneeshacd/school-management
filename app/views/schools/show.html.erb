<div class="card">
  <div class="card-header">
    <%= @school.name %>
  </div>
  <div class="card-body">
    <!-- <h5 class="card-title">Special title treatment</h5> -->
    <p class="card-text"><%= @school.description %></p>
    <p>Enrollment details: <%= current_user.student_enrollment.batch.course.name %>, <%= current_user.student_enrollment.batch.name%></p>
    <a href=<%= edit_school_path(@school)%> class="btn btn-success">Edit</a>
  </div>
</div>

