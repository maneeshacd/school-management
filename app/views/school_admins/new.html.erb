<div class="container mt-5">
  <div class="card">
    <div class="card-header">
        <h3><%= @school.name %> : New School Admin</h3>
    </div>
    <div class="card-body">
      <%= render 'form', school_admin: @school_admin %>
      <hr>
      <td><%= link_to "<< Back", school_school_admins_path, class: 'btn btn-primary btn-sm ' %></td>
    </div>
  </div>
</div>
