<div class="container mt-5">
  <div class="card">
    <div class="card-header">
        <h3> New Batch </h3>
    </div>
    <div class="card-body">
      <%= render 'form', batch: @batch, url: course_batches_path(@course), _method: :post %>
      <hr>
      <td><%= link_to "<< Back", course_batches_path(@course), class: 'btn btn-primary btn-sm ' %></td>
    </div>
  </div>
</div>
